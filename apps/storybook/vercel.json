{
  "framework": null,
  "installCommand": "cd ../.. && rm -rf node_modules/.package-lock.json && npm ci --ignore-scripts && npm rebuild",
  "buildCommand": "npm run build",
  "outputDirectory": "storybook-static"
}
